{
  "name": "ispy-root",
  "version": "1.0.0",
  "scripts": {
    "install:all": "npm install && cd frontend && npm install && cd ../backend && pip install -r requirements.txt",
    "start:frontend": "cd frontend && npm run dev",
    "start:backend": "nodemon --watch backend -e py --exec \"python backend/main.py\"",
    "start:tunnel": "lt --port 6005 --subdomain ispy-zoom982",
    "dev": "concurrently \"npm run start:backend\" \"npm run start:frontend\" \"npm run start:tunnel\""
  },
  "dependencies": {
    "concurrently": "^8.2.2",
    "localtunnel": "^2.0.2",
    "nodemon": "^3.1.0"
  }
}
